# Local dev settings — copy to `.env.local` (git-ignored) and fill in.
#   cp .env.local.example .env.local
# Picked up automatically by `mise run sandbox`. Keep the quotes.

# Signing identity for LOCAL sandboxed test builds (from `security find-identity -v -p codesigning`).
# Leave unset to sign ad-hoc (still sandboxed, no cert needed).
APPLE_DEV_IDENTITY="Apple Development: JOAO P P TEIXEIRA (N84NX6YKEX)"
